import UserDetailsPage from '@/app/(dashboard)/agent/tenants/[id]/page';
import React from 'react';

const page = () => {
    return (
        <div>
            <UserDetailsPage />
        </div>
    );
};

export default page;